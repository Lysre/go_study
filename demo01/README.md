# TCP Demo
## 通信协议
- 需要注意MSS分段大小
  - MSS= MTU - ip首部 - tcp首部，MTU根据链路接口层的不同而不同
  - TCP传输的时候需要双方协商MSS值
  - 应用层传输数据大于MSS时，需要将数据分段传输
  - 报文格式(segment)：TCP首部 + TCP数据[不能超过MSS]
- 链路层的MTU大小

## 长连接
- 长连接是指客户端和服务器之间保持连接状态，直到客户端主动断开连接
- 长连接可以提高传输效率，减少连接建立和断开的开销
- 长连接需要客户端和服务器双方都支持长连接

## 短连接
- 短连接是指客户端和服务器之间每次传输数据后，立即断开连接
- 短连接需要客户端和服务器双方都支持短连接

## tcp粘包
- 应用层的一条完整数据被称为报文
- TCP是面向字节流的，一次Read到的数据可能包含了多个报文，也可能只包含了半个报文，一条报文在什么地方结束需要通信双方事先约定
- UDP是面向报文的，一次Read只读一个报文，如果没有把一个报文读完，后面的内容容易会被丢弃掉，下次就读不到了，同时remoteAddr


## tcp拆包
### 数据流自定义分隔符
- 情况1：分隔符在segment中间
- 情况2：一个segment有多个分隔符，一个分隔符还横跨2个segment
- 情况3：segment里包含不完整的segment，还有一个分隔符还在segment末尾
- 情况4：分隔符在segment起始位置